# Cal Dining 双语菜单 · 使用手册

项目位置：`桌面 / Claude Project / cal-dining`

---

## 目录

- [一、马上在 Mac 上看看](#一马上在-mac-上看看)
- [二、放到手机上（只做一次）](#二放到手机上只做一次)
- [三、加到手机主屏幕](#三加到手机主屏幕)
- [四、每周更新菜单](#四每周更新菜单)
- [五、怎么用这个 app](#五怎么用这个-app)
- [六、出问题了怎么办](#六出问题了怎么办)

---

## 一、马上在 Mac 上看看

**双击 `打开菜单.command`**

浏览器会自己打开，菜单就出来了。

> 第一次双击可能弹「无法打开，因为来自身份不明的开发者」。
> 解决：右键点这个文件 → 选「打开」→ 再点一次「打开」。之后双击就正常了。

看完了按 `Control + C`，或者直接关掉那个黑色终端窗口。

> ❌ **别去双击 `index.html`。**
> 那样打开是空白页 —— 浏览器直接打开本地网页文件时，
> 出于安全限制读不了菜单数据。必须走上面那个 `.command`。

⚠️ 这个只能在 Mac 上看。**手机上要用，得先做第二步。**

---

## 二、放到手机上（只做一次）

手机要能随时打开，得把网页放到 GitHub 上。全程在网页上操作，不用命令行。
大概 10 分钟。

### 1. 注册 / 登录 GitHub

打开 [github.com](https://github.com)，没账号就注册一个（免费）。

### 2. 新建一个仓库

- 点右上角 `+` → **New repository**
- **Repository name** 填：`cal-dining`
- 选 **Public**（必须公开，Pages 免费版才能用）
- 下面的 "Add a README file" **不要勾**
- 点 **Create repository**

### 3. 上传文件

新仓库页面上有一行小字 **uploading an existing file**，点它。

然后打开访达，进到 `桌面 / Claude Project / cal-dining`，
**全选里面所有东西**（`Command + A`），拖进浏览器那个上传框。

需要上传的东西：

```
index.html
app.js
style.css
sw.js
manifest.json
scrape.py
make_icons.py
README.md
使用手册.md
data/          ← 整个文件夹
icons/         ← 整个文件夹
```

那两个 `.command` 文件传不传都行，不影响手机。

传完了，页面最下面点绿色的 **Commit changes**。

### 4. 打开 GitHub Pages

- 仓库页面上方点 **Settings**
- 左侧菜单找到 **Pages**
- **Source** 选 `Deploy from a branch`
- **Branch** 选 `main`，右边文件夹选 `/ (root)`
- 点 **Save**

等 1–2 分钟，刷新一下 Pages 这个页面，上面会出现一个网址：

```
https://你的用户名.github.io/cal-dining/
```

**这个网址就是你的 app，记下来。**

---

## 三、加到手机主屏幕

用 **iPhone 的 Safari**（必须是 Safari，Chrome 加出来的没有效果）：

1. 打开上面那个网址
2. 点底部中间的**分享按钮**（方框里一个向上箭头）
3. 往下滑，点 **添加到主屏幕**
4. 名字可以改成「食堂菜单」，点**添加**

主屏幕上就多了个图标（蓝底金碗）。点开是全屏的，
**没有 Safari 的地址栏**，跟正经 app 一样。

> 📶 **第一次打开时要联网**，它会把菜单存到手机本地。
> 存好之后，**没信号也能打开**，走在路上随时看。

---

## 四、每周更新菜单

食堂菜单每周变，所以每周日要更新一次。

### 第 1 步：抓新菜单

**双击 `每周更新.command`**

等 20 秒左右，它会自己跑完，然后告诉你两种结果之一：

**情况 A：「✅ 没有新菜要翻译」**
→ 直接跳到第 3 步。

**情况 B：「📝 有 N 条新词要翻译」**
→ 继续第 2 步。

> ⚠️ 如果看到 **❌** 或者一堆 **⚠️** 报警，**先别上传**，
> 把终端里的内容截图发给 Claude。

### 第 2 步：翻译新菜（只有情况 B 才需要）

脚本会自动帮你打开 `data/missing.json`，里面长这样：

```json
{
  "dishes": {
    "Tarka Dal Stew": { "zh": "", "note": "" },
    "Kachumber Salad": { "zh": "", "note": "" }
  },
  "stations": {}
}
```

**全选复制**这段内容，发给 Claude，说：

> 按 glossary 格式帮我填 zh 和 note。
> note 里写清主料、做法、辣不辣、油不油。

Claude 会把填好的还给你，比如：

```json
{
  "dishes": {
    "Tarka Dal Stew": {
      "zh": "印度香煎扁豆糊",
      "note": "Tarka 指最后浇一勺爆香的香料油，豆糊绵软，微辣"
    }
  }
}
```

然后用**文本编辑**打开 `data/glossary.json`，把这些新条目
**粘到 `"dishes": {` 后面**（注意每条之间要有逗号）。存盘。

### 第 3 步：传到 GitHub

打开你的仓库网页 → 进 `data` 文件夹 → 点 **Add file** → **Upload files**

把这两个文件拖进去：

- `data/menu.json` ← **每次都要传**
- `data/glossary.json` ← 只有第 2 步改过才要传

点 **Commit changes**。

> GitHub 会问是不是要覆盖同名文件，确认覆盖就对了。

### 第 4 步：手机刷新

打开手机上的 app，**下拉刷新一下**。新菜单就出来了。

---

**整个流程熟练之后 3–5 分钟。**

---

## 五、怎么用这个 app

打开就是**今天** + **Crossroads**（你上次选的食堂会被记住）。

- **最上面一行**：切换日期，本周 7 天。金色下划线的是今天。
- **第二行**：切换食堂。**变灰的**表示那天不开门。
- **中间**：早餐 / 午餐 / 晚餐，点标题可以展开收起。
  每一餐右边写着**营业时间**，还有一个状态标签：

  | 标签 | 意思 |
  |---|---|
  | 🟢 **供餐中** | 现在正在供应，去就能吃上 |
  | 🟠 **还有 15 分钟结束** | 快关门了，剩不到半小时才会显示 |
  | 🟠 **30 分钟后开饭** | 还没开，但快了（一小时内） |
  | ⚪️ 未开始 / 已结束 | 这顿不在饭点上 |

  **打开 app 时会自动展开你现在该看的那一餐** —— 正在供餐的优先，
  没有就展开下一顿。所以多数时候点开就是你要的，不用自己找。

  状态标签每分钟自己更新，页面开着不动也不会显示过期信息。

  > 只有看**今天**时才有状态标签。看周四的菜单只显示营业时间 ——
  > 那天还没到，说「供餐中」就是胡说了。
- **每道菜**：
  - **英文在最上面**（最大最粗）—— 到了食堂拿它跟档口牌子对
  - 中文在下面
  - 灰色小字是说明：主料、做法、辣不辣、油不油
  - 再下面是标签：`纯素` `清真` 这种绿色的是膳食标签，
    灰色的是过敏原（`小麦` `奶` `花生`…）和碳足迹

看到某道菜只有英文 + 一个虚线框的 **待译**，说明术语表里还没有它，
下次更新时会出现在 `missing.json` 里等你补。

页面最底下写着数据是什么时候抓的。**超过一周会变红提醒你该更新了。**

---

## 六、出问题了怎么办

**双击 .command 弹「身份不明的开发者」**
→ 右键点文件 → 选「打开」→ 再点一次「打开」

**手机上打开是白屏 / 打不开**
→ 检查 GitHub Pages 那个网址对不对；刚开 Pages 要等 1–2 分钟才生效

**手机上菜单还是旧的**
→ 在 Safari 里把页面下拉刷新。还不行就把 app 从后台划掉重开

**改了 `app.js` 或 `style.css`，手机上没变化**
→ 刷新两次。这是故意的：为了能离线用，网页文件会先用手机里存的那份，
后台悄悄更新，第二次刷新才用上新的。
**菜单数据不受影响，传上去刷新一次就生效。**

**终端报 ⚠️ 日期对不上**
→ 抓到了旧缓存，等几分钟再双击一次 `每周更新.command`

**终端报 ❌ 一道菜都没解析出来**
→ Cal Dining 网站改版了，`scrape.py` 要改。
把报错发给 Claude。**这种情况下原来的 menu.json 不会被破坏**，
手机上还是能看到上周的菜单。

**Café 3 一直不出现**
→ 正常，它暑假关门。秋季开学恢复供餐后会自动出现，不用改任何东西。
